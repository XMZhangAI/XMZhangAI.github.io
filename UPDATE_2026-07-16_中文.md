# 2026-07-16 MetaMind 专题与统计修复

这是本次完整更新的快速验收清单。网页正文和档案仍全部使用英文；本文件仅用于部署与测试。

## 已完成

1. **统计 0 数据根因修复**：确认线上构建未注入 `PUBLIC_ANALYTICS_ENDPOINT`；修正 GitHub Actions，并加入生产发布防呆。
2. **统计传输加固**：`fetch + keepalive` 为主，`sendBeacon` 为回退；Worker 同时解析 JSON 文本请求。
3. **三幅论文图完全重绘**：
   - MetaMind 三阶段架构：输入、候选心智状态、规范修正、验证门、记忆回写；
   - 跨模型可迁移性：原始 10 个 backbone 与 4 个高级推理扩展分开呈现，并明确完整研究覆盖 16+ 模型；
   - 六维能力轮廓：以点状区间展示 GPT-4 / MetaMind / human reference 与各维增量。
4. **Section 间距修复**：标题编号与 H2 由异常的大空白改为紧密的视觉组。
5. **2026 相关性说明**：开篇明确区分“旧 checkpoint”与仍未过时的系统研究问题。
6. **公式专业化**：Stage 2/3 使用 KaTeX 静态渲染，并补齐 `u_t`、`C_t`、`M_t`、`H_t`、`D`、`h̃_i`、`λ`、`β`、`τ` 等 notation。
7. **删除不必要内容**：移除 `Why the Spotlight matters` 与认知稿 `Epistemic boundary`。
8. **认知稿重构**：标题改为 `From cognitive scaffolds to evolvable capability.`，第一节改为总结型命题。
9. **Scale × structure**：加入 agentic control、test-taker trap、patterns-as-the-next-token 论证和 Xuanming 研究链路索引。
10. **认知架构**：加入 harness、显式/隐式双记忆、七模块认知架构与 cognitive inheritance。
11. **自进化框架**：以环境、互动、状态、学习算子、证据门和能力更新构成可证伪闭环，并解释 reciprocal modeling。
12. **认知世界模型**：明确物理因果与认知因果的双层 world model，连接 MarioLM 与 long-horizon evaluation。
13. **Super-world games**：按过去—现在—未来重构，提出玩家可定义规则、环境、社会制度乃至世界本体的平行宇宙产品与研究框架。
14. **图片比例修复**：博客图片全部保留自然宽高，不再强制方形裁切或拉伸。
15. **分享卡同步**：认知稿 OG 图改为 evolvable capability，新增 MarioLM 专属卡，并更新研究关键词。

## 本地一键检查

macOS / Linux：

```bash
./CHECK_WEBSITE_MAC_LINUX.command
```

Windows：

```bat
CHECK_WEBSITE_WINDOWS.bat
```

也可手动运行：

```bash
npm install
npm run verify
npm run preview:dist
```

打开终端显示的 `http://127.0.0.1:4173/`。

## 发布顺序

### A. 先更新统计 Worker

在仓库根目录运行：

```bash
cd optional/analytics-worker
npm install
npx wrangler whoami
npm run deploy
```

本包保留了现有 D1 `database_id`；Cloudflare 中已经设置的 `IP_SALT`、`ADMIN_USER`、`ADMIN_PASSWORD` secrets 不会因重新部署丢失。部署完成后打开终端给出的 Worker 地址并追加 `/health`，应看到：

```json
{"ok":true,"service":"xmz-research-analytics","database":"ready"}
```

采集地址是在同一域名后追加 `/collect`。

### B. 再确认 GitHub 变量

GitHub 仓库：

`Settings → Secrets and variables → Actions → Variables`

必须存在：

```text
Name:  PUBLIC_ANALYTICS_ENDPOINT
Value: https://你的-worker.workers.dev/collect
```

### C. 发布主页

把本包代码提交到仓库后，运行 `Actions → Deploy research website → Run workflow`。新版工作流会依次检查：

1. 变量存在且是以 `/collect` 结尾的 HTTPS 地址；
2. Worker `/health` 可访问；
3. D1 健康（新版 Worker）；
4. `ALLOWED_ORIGINS` 允许主页域名；
5. Astro 构建与所有页面引用通过。

任何一项错误都会停止发布并给出具体原因，不再产生“页面正常但统计为 0”的版本。

完整后台部署、IP/浏览记录、DNT、CORS 和验收方法见 `ANALYTICS_BACKEND_中文.md`。

## 已执行验证

- Astro：35 个文件，0 error / 0 warning / 0 hint；
- 静态站：14 个 HTML、140 个本地引用，0 missing；
- Open Graph：主页与专题图均为 1200 × 630；
- CV：未进入公开构建；
- Service Worker：未重新注册；
- Cloudflare Worker：TypeScript typecheck 通过；
- 生产统计防呆：有效端点通过，空端点按设计失败。
