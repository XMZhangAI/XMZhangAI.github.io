<figure class="essay-figure native-figure full-bleed superworld-visual">
  <div class="world-plate">
    <header><span>Super-world games</span><strong>Worlds change function.</strong></header>

    <div class="substrate" aria-label="Generative substrate">
      <strong>Physical world</strong><i>×</i><strong>Cognitive cause</strong><i>×</i><strong>Adaptive engine</strong>
    </div>

    <div class="time-line">
      <details>
        <summary><b>Past</b><h3>Observe</h3><strong>Worlds as data engines.</strong><span>simulate → measure → explain</span></summary>
        <p>Populate bounded sandboxes with agents and use emergent behavior as a scientific instrument.</p>
      </details>
      <details>
        <summary><b>Present</b><h3>Calibrate</h3><strong>Worlds as feedback systems.</strong><span>act → observe → update → retest</span></summary>
        <p>Connect action to persistent state and compare predicted trajectories with real interaction.</p>
      </details>
      <details>
        <summary><b>Future</b><h3>Invent</h3><strong>Worlds as engines of new knowledge.</strong><span>inherit → mutate → select → translate</span></summary>
        <p>Let rules, institutions, species, economies, and relationships evolve beyond today's priors.</p>
      </details>
    </div>

    <div class="definition"><span>Super-world game</span><p>A causally coherent universe whose environment, rules, institutions, interactions, and ontology can be authored and evolved.</p></div>
  </div>
</figure>

<style>
  .superworld-visual { padding: 0 !important; background: transparent !important; border: 0 !important; }
  .world-plate { --ink: #121716; --paper: #f3f0e8; --aqua: #16a7a7; --gold: #d69b3d; padding: clamp(28px,4vw,56px); color: var(--ink); background: #f4eee4; border-top: 1px solid var(--ink); border-bottom: 1px solid var(--ink); font-family: var(--sans); }
  header { display: grid; grid-template-columns: 180px 1fr; gap: 24px; align-items: baseline; }
  header span { color: #6c6e66; font-size: .56rem; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
  header strong { font-family: var(--serif); font-size: clamp(2rem,4.4vw,4.25rem); font-weight: 430; line-height: .95; letter-spacing: -.035em; }
  .substrate { margin-top: clamp(38px,6vw,72px); padding: 17px 0; display: flex; align-items: center; justify-content: center; gap: clamp(12px,3vw,34px); border-top: 1px solid rgba(18,23,22,.25); border-bottom: 1px solid rgba(18,23,22,.25); }
  .substrate strong { font: 470 clamp(.85rem,1.4vw,1.1rem)/1.2 var(--serif); }
  .substrate i { color: var(--gold); font-style: normal; }
  .time-line { position: relative; margin: 58px 0 72px; display: grid; grid-template-columns: repeat(3,1fr); gap: clamp(28px,5vw,72px); isolation: isolate; }
  .time-line::before { content: ''; position: absolute; z-index: -1; top: 42px; right: 8%; left: 8%; height: 1px; background: linear-gradient(90deg,var(--aqua),var(--aqua) 55%,var(--gold)); transform: rotate(-4deg); }
  .time-line details { position: relative; padding-top: 22px; }
  .time-line details:nth-child(1) { transform: translateY(38px); }
  .time-line details:nth-child(2) { transform: translateY(19px); }
  .time-line details::before { content: ''; position: absolute; top: 35px; left: 0; width: 12px; height: 12px; background: #f4eee4; border: 2px solid var(--aqua); border-radius: 50%; box-shadow: 0 0 0 8px rgba(22,167,167,.08); animation: world-node 3.7s ease-in-out infinite; }
  .time-line details:nth-child(2)::before { animation-delay: -1.2s; }
  .time-line details:nth-child(3)::before { border-color: var(--gold); animation-delay: -2.4s; }
  .time-line summary { min-height: 245px; display: flex; flex-direction: column; padding: 68px 0 20px; cursor: pointer; list-style: none; border-bottom: 1px solid rgba(18,23,22,.25); }
  .time-line summary::-webkit-details-marker { display: none; }
  .time-line summary::after { content: '+'; position: absolute; right: 0; bottom: 20px; width: 25px; height: 25px; display: grid; place-items: center; border: 1px solid rgba(18,23,22,.28); border-radius: 50%; transition: transform .2s ease,background .2s ease,color .2s ease; }
  .time-line details[open] summary::after { color: #fff; background: var(--ink); transform: rotate(45deg); }
  .time-line b { color: var(--aqua); font-size: .54rem; letter-spacing: .1em; text-transform: uppercase; }
  .time-line h3 { margin: 9px 0 25px; font: 470 clamp(1.8rem,3vw,2.75rem)/.95 var(--serif); }
  .time-line summary > strong { margin-top: auto; max-width: 240px; font: 470 clamp(1.1rem,1.8vw,1.45rem)/1.08 var(--serif); }
  .time-line span { margin-top: 18px; color: #59635f; font-size: .52rem; font-weight: 820; letter-spacing: .06em; line-height: 1.5; text-transform: uppercase; }
  .time-line p { margin: 16px 0 0; color: #47514e; font-size: .68rem; line-height: 1.55; }
  .definition { padding: clamp(26px,4vw,48px); display: grid; grid-template-columns: 180px 1fr; gap: 30px; color: #fff; background: var(--ink); }
  .definition span { color: #69d0d4; font-size: .56rem; font-weight: 800; letter-spacing: .1em; text-transform: uppercase; }
  .definition p { max-width: 760px; margin: 0; font: 440 clamp(1.35rem,2.5vw,2.2rem)/1.18 var(--serif); }
  @keyframes world-node { 50% { transform: scale(1.3); box-shadow: 0 0 0 13px rgba(22,167,167,.03); } }
  @media (max-width: 760px) {
    header { grid-template-columns: 1fr; }
    .substrate { align-items: flex-start; flex-direction: column; }
    .time-line { padding-left: 18px; grid-template-columns: 1fr; gap: 25px; }
    .time-line::before { top: 0; bottom: 0; left: 23px; width: 1px; height: auto; transform: none; }
    .time-line details, .time-line details:nth-child(1), .time-line details:nth-child(2) { padding-left: 30px; transform: none; }
    .time-line details::before { top: 35px; left: -1px; }
    .time-line summary { min-height: 220px; }
    .definition { grid-template-columns: 1fr; }
  }
</style>
