<figure class="essay-figure native-figure full-bleed cognition-architecture">
  <div class="system-shell">
    <header>
      <div><span>Cognitive architecture / working hypothesis</span><strong>Intelligence is a coordinated system, not a larger answer key.</strong></div>
      <small>Information becomes action through inherited, revisable operations.</small>
    </header>

    <div class="architecture-map">
      <div class="world input"><b>World</b><span>signals · people · constraints</span></div>
      <div class="module"><b>01</b><strong>Perception</strong><span>encode what changes</span></div>
      <div class="module"><b>02</b><strong>Attention</strong><span>allocate scarce computation</span></div>
      <div class="module double"><b>03</b><strong>Memory</strong><span><i>explicit</i> events & facts</span><span><i>implicit</i> revisable latent state</span></div>
      <div class="module"><b>04</b><strong>Reasoning</strong><span>infer under uncertainty</span></div>
      <div class="module"><b>05</b><strong>Social cognition</strong><span>model minds, norms & relations</span></div>
      <div class="module wide"><b>06</b><strong>Executive functions & generation</strong><span>plan · decide · act</span></div>
      <div class="world output"><b>Changed world</b><span>consequence · feedback · new evidence</span></div>
      <div class="meta-loop"><b>07 / Metacognition & learning</b><span>monitor error</span><i>→</i><span>revise state</span><i>→</i><span>update policy</span><i>→</i><span>transfer cognition</span></div>
    </div>

    <footer>
      <strong>Cognitive inheritance</strong>
      <p>Civilization advances when useful ways of perceiving, reasoning, coordinating, and creating survive the individual who discovered them.</p>
    </footer>
  </div>
  <figcaption><strong>A cognitive stack for aligned agency.</strong> Knowledge enters through perception, but reliable action requires attention, dual memory, uncertain reasoning, social cognition, executive control, and a metacognitive learning loop. The architecture is a research program: each interface can be instrumented, intervened on, and falsified.</figcaption>
</figure>

<style>
  .cognition-architecture { padding: 0 !important; background: transparent !important; border: 0 !important; }
  .system-shell { padding: clamp(22px, 3.2vw, 42px); color: #f5f0e8; background: #11102a; border: 1px solid #34304d; box-shadow: 0 28px 70px rgba(17,16,42,.17); font-family: var(--sans); }
  header { padding-bottom: 25px; display: flex; align-items: end; justify-content: space-between; gap: 30px; border-bottom: 1px solid rgba(255,255,255,.17); }
  header > div { display: grid; gap: 8px; }
  header span, header small { color: #948da8; font-size: .56rem; font-weight: 780; letter-spacing: .11em; line-height: 1.5; text-transform: uppercase; }
  header strong { font-family: var(--serif); font-size: clamp(1.5rem,3vw,2.75rem); font-weight: 410; line-height: 1.05; letter-spacing: -.025em; }
  header small { max-width: 250px; text-align: right; }
  .architecture-map { margin-top: 28px; display: grid; grid-template-columns: repeat(8,minmax(118px,1fr)); gap: 8px; overflow-x: auto; }
  .world, .module { position: relative; min-height: 150px; padding: 18px; display: flex; flex-direction: column; background: #191735; border: 1px solid #383451; }
  .world { justify-content: end; background: #ffbe5c; color: #171327; border-color: #ffbe5c; }
  .module::after, .world.input::after { content: '→'; position: absolute; z-index: 2; top: 50%; right: -14px; color: #ffbe5c; font: 800 12px/1 var(--sans); transform: translateY(-50%); }
  .world.output::after { display: none; }
  .module b { color: #ffbe5c; font-size: .55rem; letter-spacing: .09em; }
  .module strong { margin: auto 0 12px; font-family: var(--serif); font-size: 1.25rem; font-weight: 430; line-height: 1.05; }
  .module span, .world span { color: #9d97af; font-size: .54rem; line-height: 1.5; }
  .module.double span { margin-top: 4px; }
  .module i { color: #ffbe5c; font-style: normal; }
  .world b { margin-bottom: 9px; font-family: var(--serif); font-size: 1.3rem; font-weight: 550; }
  .world span { color: rgba(23,19,39,.68); }
  .meta-loop { grid-column: 2 / 8; margin-top: 3px; padding: 17px 20px; display: grid; grid-template-columns: 1.4fr 1fr auto 1fr auto 1fr auto 1fr; gap: 12px; align-items: center; color: #bcb6c9; background: #24203d; border: 1px solid #504969; }
  .meta-loop b { color: #ffbe5c; font-size: .57rem; letter-spacing: .08em; text-transform: uppercase; }
  .meta-loop span { font-size: .55rem; }
  .meta-loop i { color: #ffbe5c; font-style: normal; }
  footer { margin-top: 22px; padding: 20px 0 0; display: grid; grid-template-columns: 170px 1fr; gap: 24px; border-top: 1px solid rgba(255,255,255,.17); }
  footer strong { color: #ffbe5c; font-size: .58rem; letter-spacing: .09em; text-transform: uppercase; }
  footer p { margin: 0; color: #9d97af; font-size: .6rem; line-height: 1.55; }
  @media (max-width: 860px) {
    header { align-items: start; flex-direction: column; }
    header small { text-align: left; }
    .architecture-map { grid-template-columns: 1fr; }
    .world, .module, .module.wide, .meta-loop { grid-column: 1; min-height: 125px; }
    .module::after, .world.input::after { top: auto; right: 50%; bottom: -14px; transform: translateX(50%) rotate(90deg); }
    .meta-loop { grid-template-columns: 1fr; }
    .meta-loop i { transform: rotate(90deg); }
    footer { grid-template-columns: 1fr; }
  }
</style>
