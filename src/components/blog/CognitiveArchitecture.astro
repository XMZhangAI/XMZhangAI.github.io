<figure class="essay-figure native-figure full-bleed cognition-architecture">
  <div class="architecture-plate">
    <header>
      <span>Cognitive architecture</span>
      <strong>Intelligence is coordination.</strong>
    </header>

    <div class="architecture-map" role="img" aria-label="World signals pass through six coordinated cognitive functions, then return as action to a changed world. Metacognition monitors and updates the whole system.">
      <div class="world world--input"><i></i><strong>World</strong></div>

      <div class="neural-field">
        <div class="module module--perception"><b>01</b><strong>Perception</strong></div>
        <div class="module module--attention"><b>02</b><strong>Attention</strong></div>
        <div class="module module--memory"><b>03</b><strong>Memory</strong></div>
        <div class="module module--reasoning"><b>04</b><strong>Reasoning</strong></div>
        <div class="module module--social"><b>05</b><strong>Social cognition</strong></div>
        <div class="module module--executive"><b>06</b><strong>Executive action</strong></div>
        <div class="cognitive-core"><i></i><span>coordinate</span></div>
      </div>

      <div class="world world--output"><i></i><strong>Changed<br />world</strong></div>
      <div class="meta-loop"><strong>Metacognition & learning</strong><span>monitor → revise → update → transfer</span></div>
    </div>
  </div>
</figure>

<style>
  .cognition-architecture { padding: 0 !important; background: transparent !important; border: 0 !important; }
  .architecture-plate { --ink: #121716; --paper: #f3f0e8; --aqua: #16a7a7; --gold: #d69b3d; padding: clamp(28px,4vw,56px); color: var(--ink); background: var(--paper); border-top: 1px solid var(--ink); border-bottom: 1px solid var(--ink); font-family: var(--sans); }
  header { display: grid; grid-template-columns: 180px 1fr; gap: 24px; align-items: baseline; }
  header span { color: #5f6b68; font-size: .56rem; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
  header strong { font-family: var(--serif); font-size: clamp(2rem,4.4vw,4.25rem); font-weight: 430; line-height: .95; letter-spacing: -.035em; }
  .architecture-map { position: relative; margin-top: clamp(42px,6vw,76px); display: grid; grid-template-columns: 120px minmax(0,1fr) 140px; grid-template-rows: minmax(480px,54vw) auto; gap: 28px; align-items: center; isolation: isolate; }
  .architecture-map::before { content: ''; position: absolute; z-index: -1; top: 48%; right: 70px; left: 60px; height: 1px; background: linear-gradient(90deg,var(--gold),var(--aqua) 23% 77%,var(--ink)); }
  .architecture-map::after { content: ''; position: absolute; z-index: 3; top: calc(48% - 4px); left: 62px; width: 8px; height: 8px; background: var(--gold); border-radius: 50%; box-shadow: 0 0 0 8px rgba(214,155,61,.12); animation: architecture-signal 6.2s ease-in-out infinite; }
  .world { position: relative; z-index: 2; display: grid; justify-items: center; gap: 14px; text-align: center; }
  .world i { width: 52px; height: 52px; display: block; background: var(--paper); border: 1px solid currentColor; border-radius: 50%; box-shadow: inset 0 0 0 11px var(--paper),inset 0 0 0 12px currentColor; }
  .world strong { font: 460 clamp(1.05rem,1.8vw,1.45rem)/1 var(--serif); }
  .world--input { color: #a96812; }
  .world--output { color: #087a7d; }
  .neural-field { position: relative; height: 100%; min-height: 480px; background: radial-gradient(circle at center,rgba(22,167,167,.11) 0 3%,transparent 3.2% 15%,rgba(22,167,167,.12) 15.2% 15.5%,transparent 15.7% 31%,rgba(18,23,22,.11) 31.2% 31.45%,transparent 31.7%),linear-gradient(30deg,transparent 49.8%,rgba(18,23,22,.13) 49.9% 50.1%,transparent 50.2%),linear-gradient(-30deg,transparent 49.8%,rgba(18,23,22,.13) 49.9% 50.1%,transparent 50.2%),linear-gradient(90deg,transparent 49.85%,rgba(22,167,167,.16) 49.95% 50.05%,transparent 50.15%); }
  .neural-field::before { content: ''; position: absolute; inset: 11% 7%; border: 1px solid rgba(18,23,22,.18); border-radius: 50%; }
  .module { position: absolute; width: 150px; display: grid; justify-items: center; gap: 7px; text-align: center; }
  .module::before { content: ''; width: 14px; height: 14px; background: var(--paper); border: 2px solid var(--aqua); border-radius: 50%; box-shadow: 0 0 0 8px rgba(22,167,167,.09); animation: architecture-node 3.6s ease-in-out infinite; }
  .module b { color: #71807d; font-size: .5rem; letter-spacing: .1em; }
  .module strong { max-width: 145px; font: 470 clamp(1rem,1.7vw,1.42rem)/1.04 var(--serif); }
  .module--perception { top: 17%; left: 5%; }
  .module--attention { top: 0; left: 50%; transform: translateX(-50%); }
  .module--memory { top: 17%; right: 5%; }
  .module--reasoning { bottom: 17%; left: 5%; }
  .module--social { bottom: 0; left: 50%; transform: translateX(-50%); }
  .module--executive { right: 5%; bottom: 17%; }
  .module--attention::before, .module--social::before { animation-delay: -1.2s; }
  .module--memory::before, .module--reasoning::before { animation-delay: -2.4s; }
  .cognitive-core { position: absolute; top: 50%; left: 50%; width: 118px; height: 118px; display: grid; place-items: center; align-content: center; gap: 10px; background: var(--ink); border-radius: 50%; transform: translate(-50%,-50%); color: var(--paper); }
  .cognitive-core i { width: 9px; height: 9px; background: var(--gold); border-radius: 50%; box-shadow: 0 0 0 9px rgba(214,155,61,.12); animation: architecture-core 2.8s ease-in-out infinite; }
  .cognitive-core span { font-size: .48rem; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
  .meta-loop { grid-column: 2; padding: 20px 0 0; display: flex; align-items: baseline; justify-content: space-between; gap: 24px; border-top: 1px solid rgba(18,23,22,.28); }
  .meta-loop strong { color: var(--aqua); font-size: .58rem; letter-spacing: .09em; text-transform: uppercase; }
  .meta-loop span { font: 500 .72rem/1.4 var(--serif); }
  @keyframes architecture-signal { 0% { left: 62px; opacity: 0; } 12%,88% { opacity: 1; } 100% { left: calc(100% - 76px); opacity: 0; } }
  @keyframes architecture-node { 50% { transform: scale(1.28); box-shadow: 0 0 0 13px rgba(22,167,167,.03); } }
  @keyframes architecture-core { 50% { transform: scale(1.28); } }
  @media (max-width: 760px) {
    header { grid-template-columns: 1fr; }
    .architecture-map { grid-template-columns: 1fr; grid-template-rows: auto 470px auto auto; }
    .architecture-map::before, .architecture-map::after { display: none; }
    .world { grid-template-columns: auto 1fr; align-items: center; justify-items: start; text-align: left; }
    .world i { width: 38px; height: 38px; }
    .neural-field { grid-row: 2; min-height: 470px; }
    .world--output { grid-row: 3; }
    .meta-loop { grid-column: 1; flex-direction: column; }
    .module { width: 112px; }
    .module strong { font-size: .92rem; }
    .cognitive-core { width: 92px; height: 92px; }
  }
</style>
